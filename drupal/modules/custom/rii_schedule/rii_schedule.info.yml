name: RII Schedule
description: Custom module (made by Adam Laverack) to automatically schedule content moderation workflow transitions for the scheduled RII expiry system.
package: Custom

type: module
core: 8.x

dependencies:
  - scheduled_publish:scheduled_publish

